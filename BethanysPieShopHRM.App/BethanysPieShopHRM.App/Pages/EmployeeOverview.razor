@page "/employeeoverview"

<PageTitle>@Title</PageTitle>

<h2>Employee Overview</h2>

@attribute [Authorize]

@if (Employees is null)
{
    <p><em>Loading ...</em></p>
}
else
{
    <!--table class="table table-light">
        <thead>
            <tr>
                <th />
                <th>Employee Id</th>
                <th>First Name</th>
                <th>Last Name</th>
            </tr>
        </thead>
        <tbody-->
    @* @foreach (var employee in Employees)
            {
                <tr>
                    <td><img src="@($"https://gillcleerenpluralsight.blob.core.windows.net/person/{employee.EmployeeId}-small.jpg")" class="rounded-circle" /></td>
                    <td>@employee.EmployeeId</td>
                    <td>@employee.FirstName</td>
                    <td>@employee.LastName</td>
                </tr>
            } *@
    //</tbody>
    //</table>

    <EmployeeCardQuickView Employee="_selectedEmployee"></EmployeeCardQuickView>

    <div class="row">
        @foreach (var employee in Employees)
        {
            <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3">
                <ErrorBoundary>
                    <ChildContent>
                        <EmployeeCard Employee="employee" EmployeeQuickViewClicked="ShowQuickViewPopup"></EmployeeCard>
                    </ChildContent>
                    <ErrorContent>
                        <p class="errorUI">Invalid Employee!</p>
                    </ErrorContent>
                </ErrorBoundary>
            </div>
        }
    </div>
}